## basepod app update

Patch app config (strategy, healthcheck, limits, internal-only)

```
basepod app update <name> [flags]
```

### Options

```
      --cpu-pct int          CPU percent (0 = unlimited)
      --health-path string   HTTP healthcheck path
  -h, --help                 help for update
      --internal-only        Exclude from Caddy routing
      --memory-mb int        Memory limit in MB (0 = unlimited)
      --strategy string      blue_green | stop_start (default "blue_green")
```

### Options inherited from parent commands

```
      --server string   BasePod server URL (defaults to ~/.basepod/config.yaml)
```

### SEE ALSO

* [basepod app](basepod_app.md)	 - Manage apps (create, deploy, scale, env, domains, logs, rollback)

###### Auto generated by spf13/cobra on 26-May-2026
