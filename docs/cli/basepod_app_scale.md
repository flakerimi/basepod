## basepod app scale

Update the desired instance count (stored only in v1)

```
basepod app scale <name> --instances N [flags]
```

### Options

```
  -h, --help            help for scale
      --instances int   Instance count (default 1)
```

### Options inherited from parent commands

```
      --server string   BasePod server URL (defaults to ~/.basepod/config.yaml)
```

### SEE ALSO

* [basepod app](basepod_app.md)	 - Manage apps (create, deploy, scale, env, domains, logs, rollback)

###### Auto generated by spf13/cobra on 26-May-2026
