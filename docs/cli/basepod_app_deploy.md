## basepod app deploy

Deploy an app from a directory tarball or image

### Synopsis

Deploy <app> from a build context (directory or tarball path) or a pre-built
image. The server runs 'podman build' on the context, tags as basepod/<app>:<sha>,
then performs a blue/green or stop_start swap depending on the app's strategy.

Use --image to skip the build step and deploy an existing OCI image.
Use --follow to stream SSE events (build logs + deploy state) after queuing.

```
basepod app deploy <app> [path] [flags]
```

### Examples

```
  basepod app deploy my-api .
  basepod app deploy my-api ./build.tar.gz
  basepod app deploy my-api --image ghcr.io/me/api:1.2 --follow
```

### Options

```
  -f, --follow         Stream events after queuing the deploy
  -h, --help           help for deploy
      --image string   Deploy from a pre-built image instead of tarball
```

### Options inherited from parent commands

```
      --server string   BasePod server URL (defaults to ~/.basepod/config.yaml)
```

### SEE ALSO

* [basepod app](basepod_app.md)	 - Manage apps (create, deploy, scale, env, domains, logs, rollback)

###### Auto generated by spf13/cobra on 26-May-2026
