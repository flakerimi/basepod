## basepod

BasePod CLI — manage services on the local BasePod server

### Synopsis

BasePod CLI manages a BasePod server: deploy apps, attach domains,
install one-click templates, and manage credentials.

Configuration lives at ~/.basepod/config.yaml (server URL + API token, set by
'basepod login'). All commands accept --server to point at a different host.

### Examples

```
  # log in to a server
  basepod login --server http://localhost:8080

  # deploy from the current directory (tarballs the dir, ships it, builds, deploys)
  basepod app deploy myapp .

  # deploy a pre-built image
  basepod app deploy myapp --image ghcr.io/me/myapp:1.2.0

  # tail container logs
  basepod app logs myapp

  # spin up a postgres via template
  basepod template install postgres my-db POSTGRES_PASSWORD=secret

  # attach a custom domain
  basepod domain add myapp shop.example.com

  # back up server state + data
  basepod backup -o backup.tar.gz
```

### Options

```
  -h, --help            help for basepod
      --server string   BasePod server URL (defaults to ~/.basepod/config.yaml)
```

### SEE ALSO

* [basepod app](basepod_app.md)	 - Manage apps (create, deploy, scale, env, domains, logs, rollback)
* [basepod backup](basepod_backup.md)	 - Download a backup of server state (sqlite + data dir + caddy config)
* [basepod completion](basepod_completion.md)	 - Generate shell completion scripts
* [basepod domain](basepod_domain.md)	 - Manage app domains
* [basepod login](basepod_login.md)	 - Log in to a BasePod server and save the resulting token
* [basepod logout](basepod_logout.md)	 - Clear local credentials
* [basepod settings](basepod_settings.md)	 - Get/set server settings (root_domain, acme_email, admin_subdomain, dns_*)
* [basepod template](basepod_template.md)	 - Browse and install one-click apps (databases, caches, etc.)
* [basepod tokens](basepod_tokens.md)	 - Manage API tokens
* [basepod version](basepod_version.md)	 - Show CLI version

###### Auto generated by spf13/cobra on 26-May-2026
