## basepod settings

Get/set server settings (root_domain, acme_email, admin_subdomain, dns_*)

### Examples

```
  basepod settings list
  basepod settings set root_domain=example.com acme_email=ops@example.com
  basepod settings set admin_subdomain=bp
  basepod settings set dns_provider=cloudflare dns_token=xxxx     # enables wildcard certs
```

### Options

```
  -h, --help   help for settings
```

### Options inherited from parent commands

```
      --server string   BasePod server URL (defaults to ~/.basepod/config.yaml)
```

### SEE ALSO

* [basepod](basepod.md)	 - BasePod CLI — manage services on the local BasePod server
* [basepod settings list](basepod_settings_list.md)	 - Show settings
* [basepod settings set](basepod_settings_set.md)	 - Update one or more settings

###### Auto generated by spf13/cobra on 26-May-2026
